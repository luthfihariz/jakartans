<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jakartans"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sherlock.Light.DarkActionBar" >
        <activity
            android:name="com.jakartans.activities.SearchActivity"
            android:label="@string/title_activity_search" >
        </activity>
        <activity
            android:name="com.jakartans.activities.TrayekListActivity"
            android:label="@string/title_activity_trayek_list" >
        </activity>
        <activity
            android:name="com.jakartans.activities.ProfilActivity"
            android:label="@string/title_activity_profil" >
        </activity>
        <activity
            android:name="com.jakartans.activities.TrayekFullActivity"
            android:label="@string/title_activity_update_status" >
        </activity>
    </application>

</manifest>
